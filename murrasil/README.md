# مُراسِل (Murrasil)

لوحة تحكم الأخبار التقنية العربية المعتمدة على الذكاء الاصطناعي 🚀

## الإعداد السريع

1. استنسخ المشروع:

   ```bash
   git clone <repo-url>
   cd murrasil
   ```

2. أنشئ بيئة افتراضية:

   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   venv\Scripts\activate     # Windows
   ```

3. ثبّت المكتبات:

   ```bash
   pip install -r requirements.txt
   ```

4. أنشئ ملف .env من المثال:

   ```bash
   cp .env.example .env
   ```

5. احصل على مفتاح Gemini API مجاناً من:
   [Google AI Studio](https://aistudio.google.com/app/apikey)
   ثم ضعه في ملف `.env`

6. شغّل التطبيق:

   ```bash
   python main.py
   ```

7. افتح المتصفح على:
   `http://127.0.0.1:8000`

⚠️ تحذير: لا ترفع ملف `.env` أبداً على GitHub.

## المتطلبات

- Python 3+

## طريقة التثبيت والتشغيل

1. قم بتثبيت الاعتمادات:

   ```bash
   pip install -r requirements.txt
   ```

2. قم بإعداد مفاتيح التشغيل في `.env`.
3. قم بتشغيل الخادم:

   ```bash
   python main.py
   ```

4. افتح المتصفح على: `http://localhost:8000`
